# Frontend runs on Vite dev server (default port 5173).
# API requests are proxied to the backend via vite.config.ts.
# Ensure the backend is running on http://localhost:3000

# If you run the frontend on a different host and need to point to a different API:
# VITE_API_URL=http://localhost:3000
